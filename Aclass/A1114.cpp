#include <iostream>
#include <algorithm>
using namespace std;
struct node {
    int id, fid, mid, kid[5], esnum, total_area;
} ;

int main() {
    int n;
    cin >> n;
    for(int i = 0; i < n; i++) {
        int id, fid, mid, knum, esnum;
        cin >> id >> fid >> mid >> knum;
        for(int j = 0; j < knum; j++) {
            int kid;
            cin >> kid;
        }
        cin >> esnum;
        for(int j = 0; j < esnum; j++) {
           
        }
    }
} 